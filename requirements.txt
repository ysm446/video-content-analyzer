fastapi
uvicorn[standard]
# ASR: faster-whisper (CTranslate2)。CUDA を使う場合は下の nvidia-*-cu12 が必要。
faster-whisper
nvidia-cublas-cu12
nvidia-cudnn-cu12
# 翻訳/VL は llama.cpp(GGUF)。transformers は translator.py の HF フォールバック用（ピン不要）。
transformers
accelerate
# torch は translator.py / video_reviewer.py / vram.py が使用。
# Blackwell(sm_120) では cu130 ホイールが必要:
#   pip install torch --index-url https://download.pytorch.org/whl/cu130
torch
ffmpeg-python
srt
Pillow
# システムリソースモニター(/system-stats)用。pynvml は nvidia-ml-py が提供。
psutil
nvidia-ml-py
